const Consts = (function() {
	
	let playerType = {
		default: 'X',
		secondary: 'O'		
	};

	let className = {
		xValue: 'x-value',
		oValue: 'o-value'
	};

	return {
		playerType: playerType,
		className: className,
	}

}());
